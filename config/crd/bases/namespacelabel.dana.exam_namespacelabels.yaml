---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespacelabels.namespacelabel.dana.exam
spec:
  group: namespacelabel.dana.exam
  names:
    kind: NamespaceLabel
    listKind: NamespaceLabelList
    plural: namespacelabels
    singular: namespacelabel
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NamespaceLabel is the Schema for the namespacelabels API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NamespaceLabel
            properties:
              labels:
                additionalProperties:
                  type: string
                description: Labels is the map of key-value pairs that should be applied
                  to the Namespace
                type: object
            required:
            - labels
            type: object
          status:
            description: status defines the observed state of NamespaceLabel
            properties:
              applied:
                description: Applied indicates if the labels were successfully synced
                  to the Namespace
                type: boolean
              message:
                description: Message provides details about the sync status, such
                  as failure reasons
                type: string
            required:
            - applied
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
